pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
    plugins {
        id "com.chaquo.python" version "10.2.0" apply false
    }
}

rootProject.name = "FaceApp"
include(":app")
